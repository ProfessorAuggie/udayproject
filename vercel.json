{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "cd server && npm ci --legacy-peer-deps && npx prisma generate && npm run build && cd ../client && npm ci --legacy-peer-deps && npm run build && cd .. && mkdir -p public && cp -r client/dist/* public/ && echo 'Build complete'",
  "outputDirectory": "public",
  "functions": {
    "vercel/api/index.ts": {
      "memory": 512,
      "maxDuration": 30,
      "includeFiles": "server/dist/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/vercel/api"
    },
    {
      "source": "/((?!api(?:\\/|$)).*)",
      "destination": "/index.html"
    }
  ]
}
