{
  "installCommand": "npm install && cd server && npm ci && npx prisma generate && cd ../client && npm ci",
  "buildCommand": "cd server && npx prisma generate && npx prisma migrate deploy && npm run build && cd ../client && npm run build && cd .. && rm -rf public && mkdir -p public && cp -r client/dist/. public/",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/((?!api(?:\\/|$)).*)",
      "destination": "/index.html"
    }
  ]
}
